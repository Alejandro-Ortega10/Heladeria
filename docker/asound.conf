# /etc/asound.conf — Configuración ALSA mínima para Raspberry Pi en Docker
#
# (Todo el contenido ha sido comentado porque estaba causando errores
# "alsa lib unable to find definition" o "no such file or directory" 
# al intentar cargar el plugin asym en Raspberry Pi).
# Las advertencias de ALSA ya están siendo silenciadas desde el código Python.
